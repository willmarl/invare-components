5mm 4-pin RGB LED (clear lens). Longest pin is usually common cathode (GND). Each color needs its own current-limiting resistor — red has a lower forward voltage so it needs a different resistor than green/blue.

- Forward Voltage : Red ~2.0V, Green/Blue ~3.0V – 3.2V
- Forward Current : 20mA per color

> AI created desc
