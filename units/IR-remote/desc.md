Elegoo-style 21-button credit-card IR remote. Pairs with an IR receiver module (e.g. KY-022 / TSOP1838). Button hex codes vary by clone — dump them once with the IRremote library instead of assuming a table. Holding a button usually sends a repeat code (often `0xFFFFFFFF`).

- Protocol : NEC
- Carrier : 38 kHz
- Range : ~8 – 10 m (line of sight, depends on receiver)
- Battery : CR2025 3V (pull the plastic tab before first use)
- Angle : ~±60°

> AI created desc
